spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password:

service:
  weather: mock

open-weather:
  url: http://api.openweathermap.org/data/2.5/weather
  appId: TBD
